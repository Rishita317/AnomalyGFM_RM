"""Minimal stub of the `torch_geometric` package to satisfy tests that only
need the `glorot` initializer during import-time.

This is intentionally tiny and should be removed when the real
`torch_geometric` package is available in the environment.
"""
__all__ = ["nn"]
