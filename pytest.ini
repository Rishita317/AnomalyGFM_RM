[pytest]
# Suppress known DeprecationWarning coming from fontTools (third-party dependency)
# so running tests with -W error doesn't fail the test run.
filterwarnings =
    ignore::DeprecationWarning:fontTools.*
